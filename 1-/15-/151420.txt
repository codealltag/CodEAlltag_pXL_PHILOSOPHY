es gibt kein "größer als die komplexen Zahlen".
x > IC ist nicht definiert.
Definiert ist nur x > M(IC), wobei M(IC) die Mächtigkeit der Menge der 
komplexen Zahlen ist.
Die ist jedoch überabzählbar unendlich, da wird 0 kaum größer sein.